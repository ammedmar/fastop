# Development notes

Only `roadmap.md` is active.

The `archive/` directory contains completed design investigations and dated
benchmark reports. They are retained as historical evidence, not as current
package documentation. User-facing explanations belong in `docs/`, and
reproducible mathematical examples belong in `notebooks/`.
